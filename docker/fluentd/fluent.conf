<source>
 type forward
 port 24224
 bind 0.0.0.0
</source>

<filter *>
  @type concat
  key log
  stream_identity_key container_id
  multiline_start_regexp /^E,/
  multiline_end_regexp /^B/
</filter>

<match *>
  type stdout
</match>
