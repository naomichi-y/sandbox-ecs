<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match docker.*>
  @type copy

  # <store>
  #   @type stdout
  # </store>

  # <store>
  #   @type file
  #   path /home/fluent/log
  #   flush_interval 3s
  # </store>

  <store>
    @type forward
    flush_interval 3s
    heartbeat_type tcp
    heartbeat_interval 1s

    retry_wait 10s
    max_retry_wait 1h

    <server>
      host development.log.private.nichigas.co.jp
      port 24224
    </server>
  </store>
</match>
