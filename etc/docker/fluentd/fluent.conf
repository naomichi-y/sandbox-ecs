<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match docker.sandbox_ecs_rails_development>
  @type copy

  # <store>
  #   @type stdout
  # </store>

  # <store>
  #   @type file
  #   path /home/fluent/log
  #   flush_interval 10s
  # </store>

  <store>
    @type forward
    flush_interval 10s
    heartbeat_type tcp
    heartbeat_interval 1s

    retry_wait 10s
    max_retry_wait 1h

    <server>
      host internal-log-development-31264653.ap-northeast-1.elb.amazonaws.com
      port 24224
    </server>
  </store>
</match>
